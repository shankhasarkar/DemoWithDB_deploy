apiVersion: v1
kind: Service
metadata:
  name: {{ include "wfademo.fullname" . }}
  labels:
    {{- include "wfademo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wfademo.selectorLabels" . | nindent 4 }}
